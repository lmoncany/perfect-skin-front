// Content collections are intentionally empty — this is a headless site.
// All post data comes from WordPress via src/lib/content.ts at build time.
// Kept as an empty export so `astro sync` and the content types don't break.

export const collections = {};
